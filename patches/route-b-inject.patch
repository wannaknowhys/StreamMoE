diff --git a/common/CMakeLists.txt b/common/CMakeLists.txt
index 36f1e0cd5..807600820 100644
--- a/common/CMakeLists.txt
+++ b/common/CMakeLists.txt
@@ -179,3 +179,28 @@ if (LLAMA_LLGUIDANCE)
 endif()
 
 target_link_libraries(${TARGET} PUBLIC llama Threads::Threads)
+
+# StreamMoE route B: expert-pool backend sources live in the parent repo.
+set(STREAM_MOE_SRC ${CMAKE_CURRENT_SOURCE_DIR}/../../../src)
+target_include_directories(${TARGET} PRIVATE ${STREAM_MOE_SRC} ${CMAKE_CURRENT_SOURCE_DIR}/../ggml/src)
+# Feature frag include dirs are added globally by the root CMakeLists feature block.
+target_sources(${TARGET} PRIVATE
+    ${STREAM_MOE_SRC}/server/route_b_inject.cpp
+    ${STREAM_MOE_SRC}/backend/moe_backend.cpp
+    ${STREAM_MOE_SRC}/backend/minigraph_exec.cpp
+    ${STREAM_MOE_SRC}/backend/mix_split.cpp
+    ${STREAM_MOE_SRC}/backend/scatter_plan.cpp
+    ${STREAM_MOE_SRC}/backend/scheduler.cpp
+    ${STREAM_MOE_SRC}/backend/route_b_chain.cpp
+    ${STREAM_MOE_SRC}/io/staging_reader.cpp
+    ${STREAM_MOE_SRC}/loader/moe_loader.cpp
+    ${STREAM_MOE_SRC}/loader/model_builder.cpp
+    ${STREAM_MOE_SRC}/loader/topo_builder.cpp
+    ${STREAM_MOE_SRC}/pool/expert_stats.cpp)
+# Async DIO backend by platform: IOCP (Windows) vs pread/io_uring (POSIX).
+if (WIN32)
+    target_sources(${TARGET} PRIVATE ${STREAM_MOE_SRC}/io/async_dio_win.cpp)
+    target_link_libraries(${TARGET} PRIVATE ws2_32 advapi32 synchronization)
+else()
+    target_sources(${TARGET} PRIVATE ${STREAM_MOE_SRC}/io/async_dio_posix.cpp)
+endif()
diff --git a/common/arg.cpp b/common/arg.cpp
index 66dd9dd3e..502411596 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -783,6 +783,9 @@ static bool common_params_parse_ex(int argc, char ** argv, common_params_context
         std::string value;
         if (opt.get_value_from_env(value)) {
             try {
+#ifdef STREAM_MOE_ROUTE_B
+                params.route_b_track_option(opt.args, opt.env);
+#endif
                 if (opt.handler_void && is_truthy(value)) {
                     opt.handler_void(params);
                 }
@@ -837,6 +840,9 @@ static bool common_params_parse_ex(int argc, char ** argv, common_params_context
                 fprintf(stderr, "warn: %s environment variable is set, but will be overwritten by command line argument %s\n", opt.env, arg.c_str());
             }
             try {
+#ifdef STREAM_MOE_ROUTE_B
+                params.route_b_track_option(opt.args, arg);
+#endif
                 if (opt.handler_void) {
                     opt.handler_void(params);
                     continue;
@@ -888,6 +894,13 @@ static bool common_params_parse_ex(int argc, char ** argv, common_params_context
     // parse all CLI args now, so that -hf is available below for remote preset resolution
     parse_cli_args();
 
+#ifdef STREAM_MOE_ROUTE_B
+    const auto placement_error = params.route_b_placement_error();
+    if (!placement_error.empty()) {
+        throw std::invalid_argument(placement_error);
+    }
+#endif
+
     postprocess_cpu_params(params.cpuparams,       nullptr);
     postprocess_cpu_params(params.cpuparams_batch, &params.cpuparams);
 
diff --git a/common/preset.cpp b/common/preset.cpp
index 4c61e93ee..e587ff71c 100644
--- a/common/preset.cpp
+++ b/common/preset.cpp
@@ -147,6 +147,9 @@ void common_preset::apply_to_params(common_params & params, const std::set<std::
             }
         }
         // apply each option to params
+#ifdef STREAM_MOE_ROUTE_B
+        params.route_b_track_option(opt.args, std::string("preset:") + (opt.env ? opt.env : opt.args.front()));
+#endif
         if (opt.handler_string) {
             opt.handler_string(params, val);
         } else if (opt.handler_int) {
diff --git a/common/speculative.cpp b/common/speculative.cpp
index 4eef2212e..8e076fdb2 100644
--- a/common/speculative.cpp
+++ b/common/speculative.cpp
@@ -12,6 +12,8 @@
 
 #include "../src/llama-ext.h" // staging API: llama_set_embeddings_nextn / llama_get_embeddings_nextn_ith (used by MTP)
 
+#include "server/route_b_inject.h" // StreamMoE multi-model expert pools
+
 #include <algorithm>
 #include <cassert>
 #include <cstring>
@@ -2140,6 +2142,54 @@ struct common_speculative {
     std::vector<common_speculative_impl *> impl_last;
 };
 
+size_t common_speculative_n_gen_tokens(const common_speculative * spec) {
+    size_t t = 0;
+    for (const auto & impl : spec->impls) t += impl->n_gen_tokens;
+    return t;
+}
+
+size_t common_speculative_n_acc_tokens(const common_speculative * spec) {
+    size_t t = 0;
+    for (const auto & impl : spec->impls) t += impl->n_acc_tokens;
+    return t;
+}
+
+size_t common_speculative_n_acc_drafts(const common_speculative * spec) {
+    size_t t = 0;
+    for (const auto & impl : spec->impls) t += impl->n_acc_drafts;
+    return t;
+}
+
+const std::vector<size_t> & common_speculative_n_acc_tokens_per_pos(const common_speculative * spec) {
+    static thread_local std::vector<size_t> merged;
+    merged.clear();
+    size_t n = 0;
+    for (const auto & impl : spec->impls) n = std::max(n, impl->n_acc_tokens_per_pos.size());
+    merged.resize(n, 0);
+    for (const auto & impl : spec->impls) {
+        for (size_t i = 0; i < impl->n_acc_tokens_per_pos.size(); ++i) {
+            merged[i] += impl->n_acc_tokens_per_pos[i];
+        }
+    }
+    return merged;
+}
+
+void common_speculative_print_stats(const common_speculative * spec, FILE * out) {
+    const size_t n_gen    = common_speculative_n_gen_tokens(spec);
+    const size_t n_acc    = common_speculative_n_acc_tokens(spec);
+    const size_t n_drafts = common_speculative_n_acc_drafts(spec);
+    const auto & per_pos  = common_speculative_n_acc_tokens_per_pos(spec);
+    std::fprintf(out, "[spec] draft tokens = %zu, accepted = %zu (%.2f%%), draft runs = %zu\n",
+                 n_gen, n_acc, n_gen ? 100.0 * n_acc / n_gen : 0.0, n_drafts);
+    if (!per_pos.empty()) {
+        std::fprintf(out, "[spec] accepted per pos = [");
+        for (size_t i = 0; i < per_pos.size(); ++i) {
+            std::fprintf(out, "%s%zu", i ? ", " : "", per_pos[i]);
+        }
+        std::fprintf(out, "]\n");
+    }
+}
+
 static common_ngram_map get_common_ngram_map(
         common_speculative_type type,
         const common_params_speculative_ngram_map & config) {
@@ -2329,7 +2379,9 @@ common_params common_base_params_to_speculative(const common_params & params) {
         result.devices               = params_spec.devices;
         result.model                 = params_spec.mparams;
         result.n_gpu_layers          = params_spec.n_gpu_layers;
+#ifdef STREAM_MOE_ROUTE_B
         result.tensor_buft_overrides = params_spec.tensor_buft_overrides;
+#endif
 
         if (params_spec.cpuparams.n_threads > 0) {
             result.cpuparams.n_threads       = params_spec.cpuparams.n_threads;
@@ -2401,6 +2453,20 @@ common_speculative_init_result::common_speculative_init_result(
         model_path = params.speculative.draft.mparams.path;
         LOG_INF("%s: loading draft model '%s'\n", __func__, model_path.c_str());
 
+        // StreamMoE multi-model pools (docs/MULTI_MODEL_POOL.md): the draft
+#ifdef STREAM_MOE_ROUTE_B
+        // model gets its own expert pool + buft. Point the draft load at the
+        // draft pool's overrides - never reuse the main-model overrides (the
+        // same _exps pattern must not route the draft's experts to the main
+        // pool). ram_pool_mb==0 -> full residency (no eviction).
+        if (params.expert_backend) {
+            auto * ovr = stream_moe::route_b_setup(model_path.c_str(), {}, params.moe_draft_expert_pools, params.cpuparams.n_threads, true, "");
+            if (ovr) {
+                mparams.tensor_buft_overrides = ovr; // draft pool's own (terminated) array
+            }
+        }
+#endif
+
         llama_model * model_dft = llama_model_load_from_file(params.model.path.c_str(), mparams);
         if (model_dft == NULL) {
             LOG_ERR("%s: failed to load draft model, '%s'\n", __func__, model_path.c_str());
diff --git a/common/speculative.h b/common/speculative.h
index 12ae31b7d..009538c9a 100644
--- a/common/speculative.h
+++ b/common/speculative.h
@@ -3,8 +3,22 @@
 #include "llama.h"
 #include "common.h"
 
+#ifdef STREAM_MOE_ROUTE_B
+#include <cstdio>
+#include <vector>
+#endif
+
 struct common_speculative;
 
+// StreamMoE: speculative decoding statistics (per draft position).
+// Filled by the speculative engine during process()/accept(); callers (CLI /
+// server) print these after a session.
+size_t                 common_speculative_n_gen_tokens(const common_speculative * spec);
+size_t                 common_speculative_n_acc_tokens(const common_speculative * spec);
+size_t                 common_speculative_n_acc_drafts(const common_speculative * spec);
+const std::vector<size_t> & common_speculative_n_acc_tokens_per_pos(const common_speculative * spec);
+void                   common_speculative_print_stats(const common_speculative * spec, FILE * out);
+
 // comma separated list the provided types
 std::string common_speculative_type_name_str(const std::vector<enum common_speculative_type> & types);
 
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index 2309a77b5..7e550dce0 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -2511,6 +2511,9 @@ ggml_status llama_context::graph_compute(
 }
 
 llm_graph_cb llama_context::graph_get_cb() const {
+#ifdef STREAM_MOE_ROUTE_B
+    stream_moe::route_b_begin_graph();
+#endif
     return [&](const llama_ubatch & ubatch, ggml_tensor * cur, const char * name, int il) {
         if (il >= 0) {
             ggml_format_name(cur, "%s-%d", name, il);
diff --git a/src/llama-kv-cache-dsv4.cpp b/src/llama-kv-cache-dsv4.cpp
index 948d08146..7c170e4d7 100644
--- a/src/llama-kv-cache-dsv4.cpp
+++ b/src/llama-kv-cache-dsv4.cpp
@@ -947,8 +947,12 @@ llama_dsv4_comp_state::llama_dsv4_comp_state(
 
         ggml_backend_buffer_type_t buft = ggml_backend_cpu_buffer_type();
 
+        if (!offload && model.route_b_enabled() &&
+                ggml_backend_dev_type(model.dev_layer_physical(il)) != GGML_BACKEND_DEVICE_TYPE_CPU) {
+            throw std::runtime_error("RouteB: --no-kv-offload conflicts with GPU C1 compressor state at layer " + std::to_string(il));
+        }
         if (offload) {
-            auto * dev = model.dev_layer(il);
+            auto * dev = model.dev_layer_physical(il);
             buft = ggml_backend_dev_buffer_type(dev);
 
             dev_name = ggml_backend_dev_name(dev);
diff --git a/src/llama-kv-cache.cpp b/src/llama-kv-cache.cpp
index ec0f5a753..1d7907447 100644
--- a/src/llama-kv-cache.cpp
+++ b/src/llama-kv-cache.cpp
@@ -211,8 +211,12 @@ llama_kv_cache::llama_kv_cache(
 
         ggml_backend_buffer_type_t buft = ggml_backend_cpu_buffer_type();
 
+        if (!offload && model.route_b_enabled() &&
+                ggml_backend_dev_type(model.dev_layer_physical(il)) != GGML_BACKEND_DEVICE_TYPE_CPU) {
+            throw std::runtime_error("RouteB: --no-kv-offload conflicts with GPU C1 placement at layer " + std::to_string(il));
+        }
         if (offload) {
-            auto * dev = model.dev_layer(il);
+            auto * dev = model.dev_layer_physical(il);
             buft = ggml_backend_dev_buffer_type(dev);
 
             dev_name = ggml_backend_dev_name(dev);
diff --git a/src/llama-memory-recurrent.cpp b/src/llama-memory-recurrent.cpp
index e2990972e..904230938 100644
--- a/src/llama-memory-recurrent.cpp
+++ b/src/llama-memory-recurrent.cpp
@@ -82,8 +82,12 @@ llama_memory_recurrent::llama_memory_recurrent(
 
         ggml_backend_buffer_type_t buft = ggml_backend_cpu_buffer_type();
 
+        if (!offload && model.route_b_enabled() &&
+                ggml_backend_dev_type(model.dev_layer_physical(i)) != GGML_BACKEND_DEVICE_TYPE_CPU) {
+            throw std::runtime_error("RouteB: --no-kv-offload conflicts with GPU C1 recurrent state at layer " + std::to_string(i));
+        }
         if (offload) {
-            auto * dev = model.dev_layer(i);
+            auto * dev = model.dev_layer_physical(i);
             buft = ggml_backend_dev_buffer_type(dev);
 
             dev_name = ggml_backend_dev_name(dev);
diff --git a/src/llama-model-loader.cpp b/src/llama-model-loader.cpp
index 9b22cb05f..1cc71b8c6 100644
--- a/src/llama-model-loader.cpp
+++ b/src/llama-model-loader.cpp
@@ -5,6 +5,11 @@
 #include "gguf.h"
 #include "llama-hparams.h"
 #include "llama.h"
+#ifdef STREAM_MOE_ROUTE_B
+#include "tsc_timer.h"
+// v2-chunk dense takeover: route_b fills dense tensors from their strip segments.
+namespace stream_moe { bool route_b_fill_dense(const char*, struct ggml_tensor*); }
+#endif
 
 #include <algorithm>
 #include <array>
@@ -538,6 +543,9 @@ llama_model_loader::llama_model_loader(
         const llama_model_kv_override * param_overrides_p,
         const llama_model_tensor_buft_override * param_tensor_buft_overrides_p)
         : metadata(meta), set_tensor_data(set_tensor_data), set_tensor_data_ud(set_tensor_data_ud) {
+#ifdef STREAM_MOE_ROUTE_B
+    sm_tmr::timer _t("llama_model_loader ctor");
+#endif
     int trace = 0;
     if (getenv("LLAMA_TRACE")) {
         trace = atoi(getenv("LLAMA_TRACE"));
@@ -1346,6 +1354,21 @@ void llama_model_loader::done_getting_tensors(bool partial) const {
 }
 
 void llama_model_loader::init_mappings(bool prefetch, llama_mlocks * mlock_mmaps) {
+    // StreamMoE: MoE models never use mmap - experts load into the pool, dense
+    // weights are read into deterministic buffers. mmap/mlock load modes are
+    // ignored with a warning (users who don't want the pool use upstream).
+    if (use_mmap) {
+        int64_t n_expert = 0;
+        const int kid = gguf_find_key(metadata, llm_kv(LLM_KV_EXPERT_COUNT).c_str());
+        if (kid != -1 && gguf_get_kv_type(metadata, kid) == GGUF_TYPE_UINT32) {
+            n_expert = gguf_get_val_u32(metadata, kid);
+        }
+        if (n_expert > 0) {
+            LLAMA_LOG_WARN("%s: model is MoE (%lld experts) - expert weights load into the pool; mmap/mlock ignored\n",
+                    __func__, n_expert);
+            use_mmap = false;
+        }
+    }
     if (use_mmap) {
         mappings.reserve(files.size());
         mmaps_used.reserve(files.size());
@@ -1429,6 +1452,9 @@ bool llama_model_loader::load_all_data(
         llama_mlocks * lmlocks,
         llama_progress_callback progress_callback,
         void * progress_callback_user_data) {
+#ifdef STREAM_MOE_ROUTE_B
+    sm_tmr::timer _t("load_all_data");
+#endif
     if (files.empty()) {
         for (ggml_tensor * t = ggml_get_first_tensor(ctx); t != nullptr; t = ggml_get_next_tensor(ctx, t)) {
             set_tensor_data(t, set_tensor_data_ud);
@@ -1584,14 +1610,33 @@ bool llama_model_loader::load_all_data(
         } else {
             const auto & file = files.at(weight->idx);
 
-            if (ggml_backend_buffer_is_host(cur->buffer)) {
-                file->seek(weight->offs, SEEK_SET);
-                file->read_raw(cur->data, n_size);
-                if (check_tensors) {
-                    validation_result.emplace_back(std::async(std::launch::async, [cur, n_size] {
-                        return std::make_pair(cur, ggml_validate_row_data(cur->type, cur->data, n_size));
-                    }));
+#ifdef STREAM_MOE_ROUTE_B
+            // Route B owns loading: any tensor routed to a route-B buft (expert
+            // pool, C1/C2 dense, v2-chunk dense) is read by route B itself via the
+            // shared DIO engine and uploaded to its backend (host or device).
+            // Skip llama's read entirely (the pooled expert tensors even carry a
+            // dummy data pointer).
+            bool route_b_owns = false;
+            if (tensor_buft_overrides) {
+                for (const auto * o = tensor_buft_overrides; o->pattern != nullptr; ++o) {
+                    if (o->buft && cur->buffer && ggml_backend_buffer_get_type(cur->buffer) == o->buft) {
+                        route_b_owns = true;
+                        break;
+                    }
                 }
+            }
+            if (route_b_owns) {
+                stream_moe::route_b_fill_dense(cur->name, cur);
+            } else
+#endif
+            if (ggml_backend_buffer_is_host(cur->buffer)) {
+                    file->seek(weight->offs, SEEK_SET);
+                    file->read_raw(cur->data, n_size);
+                    if (check_tensors) {
+                        validation_result.emplace_back(std::async(std::launch::async, [cur, n_size] {
+                            return std::make_pair(cur, ggml_validate_row_data(cur->type, cur->data, n_size));
+                        }));
+                    }
             } else {
                 // If upload_backend is valid load the tensor in chunks to pinned memory and upload the buffers asynchronously to the GPU.
                 if (upload_backend) {
diff --git a/src/llama-model-loader.h b/src/llama-model-loader.h
index e9fe3592d..1149ae5af 100644
--- a/src/llama-model-loader.h
+++ b/src/llama-model-loader.h
@@ -43,9 +43,17 @@ struct llama_model_loader {
             }
 
             offs = gguf_get_data_offset(gguf_ctx) + gguf_get_tensor_offset(gguf_ctx, tensor_idx);
+#ifdef STREAM_MOE_ROUTE_B
+            // StreamMoE route B: overridden tensors (expert pool buft / v2-chunk
+            // dense buft) are filled from strip segments or the scheduler, never
+            // read at tensor_info offsets - so the bounds check does not apply.
+            // (The scheduler/fill read plans validate against the files instead.)
+            (void) file;
+#else
             if (offs + ggml_nbytes(tensor) < offs || offs + ggml_nbytes(tensor) > file->size()) {
                 throw std::runtime_error(format("tensor '%s' data is not within the file bounds, model is corrupted or incomplete", ggml_get_name(tensor)));
             }
+#endif
         }
     };
 
diff --git a/src/llama-model.cpp b/src/llama-model.cpp
index c34700ff5..a9fe5cc9e 100644
--- a/src/llama-model.cpp
+++ b/src/llama-model.cpp
@@ -5,6 +5,10 @@
 #include "llama-hparams.h"
 #include "llama-impl.h"
 #include "llama-mmap.h"
+#ifdef STREAM_MOE_ROUTE_B
+#include "tsc_timer.h"
+namespace stream_moe { const char * route_b_dense_device(const char * spec, int il, int n_layer_all); }
+#endif
 #include "llama-cparams.h"
 #include "llama-model-loader.h"
 
@@ -1150,6 +1154,7 @@ struct llama_model::impl {
     std::vector<layer_dev> dev_layer;
 
     bool has_tensor_overrides;
+    bool route_b_enabled = false;
 
     std::vector<float> tensor_split_owned;
 };
@@ -1162,6 +1167,16 @@ llama_model::llama_model(const llama_model_params & params) : params(params), pi
         this->params.tensor_split = pimpl->tensor_split_owned.data();
     }
     pimpl->has_tensor_overrides = params.tensor_buft_overrides && params.tensor_buft_overrides[0].pattern;
+#ifdef STREAM_MOE_ROUTE_B
+    if (params.tensor_buft_overrides) {
+        for (auto * p = params.tensor_buft_overrides; p->pattern; ++p) {
+            if (p->buft && std::strncmp(ggml_backend_buft_name(p->buft), "STREAMMOE_EXPERT", 15) == 0) {
+                pimpl->route_b_enabled = true;
+                break;
+            }
+        }
+    }
+#endif
 }
 
 llama_model::~llama_model() {
@@ -1370,6 +1385,9 @@ void llama_model_base::load_vocab(llama_model_loader & ml) {
 }
 
 bool llama_model_base::load_tensors(llama_model_loader & ml) {
+#ifdef STREAM_MOE_ROUTE_B
+    sm_tmr::timer _t("load_tensors");
+#endif
     const auto & split_mode   = params.split_mode;
     const bool use_mlock      = params.load_mode == LLAMA_LOAD_MODE_MLOCK || params.load_mode == LLAMA_LOAD_MODE_MMAP_MLOCK;
     const auto & tensor_split = params.tensor_split;
@@ -1450,6 +1468,23 @@ bool llama_model_base::load_tensors(llama_model_loader & ml) {
     const int act_gpu_layers = devices.empty() ? 0 : std::min(n_gpu_layers, n_layer_all + 1);
     auto get_layer_buft_list = [&](int il) -> llama_model::impl::layer_dev {
         const bool is_swa = il < n_layer_all && hparams.is_swa(il);
+#ifdef STREAM_MOE_ROUTE_B
+        // Dense placement owns C1 layers and the output layer (C2 tail) when
+        // --dense-placement is set (docs/DENSE_PLACEMENT.md).
+        if (params.dense_placement) {
+            const char * dn = stream_moe::route_b_dense_device(params.dense_placement, il, n_layer_all);
+            if (dn && std::string(dn) != "CPU") {
+                for (const auto & d : devices) {
+                    if (std::string(dn) == ggml_backend_dev_name(d.dev)) {
+                        LLAMA_LOG_DEBUG("load_tensors: layer %3d dense-placed on %s, is_swa = %d\n", il, dn, is_swa);
+                        return {d.dev, &pimpl->gpu_buft_list.at(d.dev)};
+                    }
+                }
+                throw std::runtime_error(format("RouteB: dense placement device '%s' not in model devices for layer %d", dn, il));
+            }
+            return {cpu_dev, &pimpl->cpu_buft_list};
+        }
+#endif
         if (il < i_gpu_start || (il - i_gpu_start) >= act_gpu_layers) {
             LLAMA_LOG_DEBUG("load_tensors: layer %3d assigned to device %s, is_swa = %d\n", il, ggml_backend_dev_name(cpu_dev), is_swa);
             return {cpu_dev, &pimpl->cpu_buft_list};
@@ -1460,10 +1495,6 @@ bool llama_model_base::load_tensors(llama_model_loader & ml) {
         return {dev, &pimpl->gpu_buft_list.at(dev)};
     };
 
-    // assign the input layer
-    // there is very little benefit to offloading the input layer, so always keep it on the CPU
-    pimpl->dev_input = { cpu_dev, &pimpl->cpu_buft_list };
-
     // assign the repeating layers to the devices according to the splits
     pimpl->dev_layer.resize(n_layer_all);
     for (int il = 0; il < n_layer_all; ++il) {
@@ -1473,6 +1504,20 @@ bool llama_model_base::load_tensors(llama_model_loader & ml) {
     // assign the output layer
     pimpl->dev_output = get_layer_buft_list(n_layer_all);
 
+    // assign the input layer
+    // Usually the input layer stays on the CPU (little benefit to offload it).
+    // With route-B dense placement the token embedding is a global dense tensor
+    // (C2) and must follow C2, else the layer-0 prologue and the attention end up
+    // on different devices and the whole head is split.
+#ifdef STREAM_MOE_ROUTE_B
+    if (params.dense_placement) {
+        pimpl->dev_input = pimpl->dev_output;
+    } else
+#endif
+    {
+        pimpl->dev_input = { cpu_dev, &pimpl->cpu_buft_list };
+    }
+
     const auto TENSOR_NOT_REQUIRED = llama_model_loader::TENSOR_NOT_REQUIRED;
 
     // create tensors for the weights
@@ -2088,10 +2133,23 @@ void llama_model::print_info() const {
     vocab.print_info();
 }
 
-ggml_backend_dev_t llama_model::dev_layer(int il) const {
+bool llama_model::route_b_enabled() const {
+    return pimpl->route_b_enabled;
+}
+
+ggml_backend_dev_t llama_model::dev_layer_physical(int il) const {
     return pimpl->dev_layer.at(il).dev;
 }
 
+ggml_backend_dev_t llama_model::dev_layer(int il) const {
+    if (route_b_enabled()) {
+        auto * dev = ggml_backend_dev_by_name("STREAMMOE");
+        if (!dev) throw std::runtime_error("RouteB model has no STREAMMOE device");
+        return dev;
+    }
+    return dev_layer_physical(il);
+}
+
 ggml_backend_dev_t llama_model::dev_output() const {
     return pimpl->dev_output.dev;
 }
@@ -2633,6 +2691,9 @@ llama_model_params llama_model_default_params() {
         /*.load_mode                   =*/ LLAMA_LOAD_MODE_AUTO,
         /*.main_gpu                    =*/ 0,
         /*.tensor_split                =*/ nullptr,
+#ifdef STREAM_MOE_ROUTE_B
+        /*.dense_placement             =*/ nullptr,
+#endif
         /*.progress_callback           =*/ nullptr,
         /*.progress_callback_user_data =*/ nullptr,
         /*.kv_overrides                =*/ nullptr,
diff --git a/src/llama-model.h b/src/llama-model.h
index 44bd96757..aa6070c50 100644
--- a/src/llama-model.h
+++ b/src/llama-model.h
@@ -705,6 +705,8 @@ struct llama_model {
     void print_info() const;
 
     ggml_backend_dev_t dev_layer(int il) const;
+    ggml_backend_dev_t dev_layer_physical(int il) const;
+    bool route_b_enabled() const;
     ggml_backend_dev_t dev_output() const;
 
     ggml_backend_buffer_type_t select_buft(int il) const;
diff --git a/src/llama.cpp b/src/llama.cpp
index 1609fec88..f5d3d826f 100644
--- a/src/llama.cpp
+++ b/src/llama.cpp
@@ -5,6 +5,9 @@
 #include "llama-chat.h"
 #include "llama-context.h"
 #include "llama-mmap.h"
+#ifdef STREAM_MOE_ROUTE_B
+#include "tsc_timer.h"
+#endif
 #include "llama-vocab.h"
 #include "llama-model-loader.h"
 #include "llama-model-saver.h"
@@ -462,6 +465,9 @@ struct llama_model * llama_load_model_from_file(
 struct llama_model * llama_model_load_from_file(
         const char * path_model,
         struct llama_model_params params) {
+#ifdef STREAM_MOE_ROUTE_B
+    sm_tmr::timer _t("llama_model_load_from_file");
+#endif
     std::vector<std::string> splits = {};
     return llama_model_load_from_file_impl(nullptr, nullptr, nullptr, path_model, splits, /*file*/ nullptr, params);
 }
