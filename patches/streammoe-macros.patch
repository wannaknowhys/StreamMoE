diff --git a/common/arg.cpp b/common/arg.cpp
index 86f8610a5..29b9df19c 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -1755,10 +1755,15 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
                                    string_format("error: unknown value for --flash-attn: '%s'\n", value.c_str()));
                            }
                        }).set_env("LLAMA_ARG_FLASH_ATTN"));
+#ifdef STREAM_MOE_PREFILL_EXPORT
+    // --export-dir / --prefill-from options
+#endif
+#ifdef STREAM_MOE_ROUTE_B
+    // --expert-backend / --moe-ram-pool / --prompt-log / --kv-placement options
+#endif
     add_opt(common_arg(
         {"-p", "--prompt"}, "PROMPT",
-        "prompt to start generation with; for system message, use -sys",
-        [](common_params & params, const std::string & value) {
+        "prompt to start generation with; for system message, use -sys",        [](common_params & params, const std::string & value) {
             params.prompt = value;
         }
     ).set_excludes({LLAMA_EXAMPLE_SERVER}));
diff --git a/common/common.cpp b/common/common.cpp
index 3d54bd600..0e1227e4e 100644
--- a/common/common.cpp
+++ b/common/common.cpp
@@ -1742,6 +1742,12 @@ struct llama_context_params common_context_params_to_llama(const common_params &
     cparams.flash_attn_type   = params.flash_attn_type;
     cparams.cb_eval           = params.cb_eval;
     cparams.cb_eval_user_data = params.cb_eval_user_data;
+#ifdef STREAM_MOE_PREFILL_EXPORT
+    // cparams.export_dir = params.export_dir;
+#endif
+#ifdef STREAM_MOE_ROUTE_B
+    // route_b_setup injection + KV placement application
+#endif
     cparams.offload_kqv       = !params.no_kv_offload;
     cparams.no_perf           = params.no_perf;
     cparams.op_offload        = !params.no_op_offload;
diff --git a/common/common.h b/common/common.h
index de49dac9f..b997574e1 100644
--- a/common/common.h
+++ b/common/common.h
@@ -438,7 +438,14 @@ struct lr_opt {
 struct ggml_opt_optimizer_params common_opt_lr_pars(void * userdata);
 
 struct common_params {
+#ifdef STREAM_MOE_PREFILL_EXPORT
+    // --export-dir / --prefill-from fields
+#endif
     int32_t n_predict             =    -1; // max. number of new tokens to predict, -1 == no limit
+
+#ifdef STREAM_MOE_ROUTE_B
+    // expert pool opt-in fields (expert_backend / moe_ram_pool_mb / ... / prompt_log_path / kv_placement)
+#endif
     int32_t n_ctx                 =     0; // context size, 0 == context the model was trained with
     int32_t n_batch               =  2048; // logical batch size for prompt processing (must be >=32 to use BLAS)
     int32_t n_ubatch              =   512; // physical batch size for prompt processing (must be >=32 to use BLAS)
diff --git a/include/llama.h b/include/llama.h
index a04177f9f..ceac188d6 100644
--- a/include/llama.h
+++ b/include/llama.h
@@ -378,6 +378,10 @@ extern "C" {
         ggml_backend_sched_eval_callback cb_eval;
         void * cb_eval_user_data;
 
+#ifdef STREAM_MOE_PREFILL_EXPORT
+        // export_dir field
+#endif
+
         enum ggml_type type_k; // data type for K cache [EXPERIMENTAL]
         enum ggml_type type_v; // data type for V cache [EXPERIMENTAL]
 
